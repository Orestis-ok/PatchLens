# PatchLens
PatchLens is a TypeScript CLI tool that reads a local git diff, classifies changed files, maps dependency relationships, identifies related tests, and produces a structured Markdown (or JSON) impact report.
